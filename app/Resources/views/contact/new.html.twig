{% extends 'base.html.twig' %}

{% block body %}
    <h1>Contact</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('homepage') }}">Accueil</a>
        </li>
    </ul>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('build/contact.js') }}"></script>
{% endblock %}